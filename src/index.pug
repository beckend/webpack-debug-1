html.mod-no-js(lang=locale || 'en')
  head
    meta(charset='UTF-8')
    meta(name='description', content=description || 'APP')
    title
        | APP
    meta(name='viewport', content='width=device-width, initial-scale=1')
  body
    //- prevent whitespaces
    div#__ROOT__
